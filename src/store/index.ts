import { createStore } from 'vuex';

// Instantiate our Vuex store
export default createStore<any>({
    state () {
        return {
        };
    },
    getters:{
        getTheme(){
        }
    }
});
